"use client";

interface GroupProps {
    id: number;
}

const Group: React.FC<GroupProps> = (props : GroupProps ) => {

    const id = props.id

    return (
        <>
        </>
    )
}

export default Group;